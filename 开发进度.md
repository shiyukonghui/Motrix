# 开发进度

> 记录 Motrix Tauri 迁移项目各阶段的开发进度与完成情况。
> 关联文档：[MIGRATION-TAURI.md](./MIGRATION-TAURI.md)（迁移总方案）、`.trae/specs/fix-phase2-issues/`（本周期修复规格）。

---

## 本次开发周期：Phase 0-2 遗留问题修复 + 前端 Web 构建兼容

### 一、背景

依据 `MIGRATION-TAURI.md` 完成 Phase 0-2 开发后，逐项审查发现 4 类遗留问题，并新建规格 `.trae/specs/fix-phase2-issues/`（spec / tasks / checklist 三件套，全部完成）：

| 问题 | 级别 | 描述 |
| --- | --- | --- |
| Windows 构建阻塞 | P0 | KGet 依赖链 `ssh2 → libssh2-sys → openssl-sys` 以 vendored 方式编译 OpenSSL，需要 perl，未安装时 `cargo check/test` 全挂 |
| HTTPS 任务进度恒 0 | P1 | `probe_content_length` 仅支持 http（手写 TCP），https 探测失败 → `totalLength` 恒 0 → 进度/速度显示失效 |
| shell 能力 command 未注册 | P2 | 前端 `remote-shell.js` 调用的 `show_item_in_folder` / `open_path` / `trash_item` 在 Rust 侧不存在 |
| 下载速度恒 0 | P3 | KGet 进度回调 speed 字段恒为 0，速度计/global-stat 的 downloadSpeed 不工作 |

> 已核实：addUri 多 URL "镜像"语义与原 Electron 版行为一致（逐 URL 独立任务），**非缺陷**，不在修复范围。

### 二、任务 1：构建环境修复（P0）

- **根因确认**：`Kget v1.7.0` 无条件依赖 `ssh2`（默认特性 `vendored-openssl`）→ `openssl-sys` vendored 编译 OpenSSL 需要 perl；Git for Windows 自带 MSYS perl 缺少 `Locale::Maketext::Simple` 模块不可用。
- **修复**：安装 **Strawberry Perl 5.42**（winget，完整 CPAN 模块）；`README-CN.md` 本地开发章节新增 Rust 构建前置说明（perl + MSVC 工具链）。
- **验证**：`cargo check` 全 workspace 通过；`cargo test --workspace` 基线全绿（75 用例）。
- **备注**：本机全局 `RUSTC_WRAPPER=sccache` 与 Trae 沙箱冲突，构建命令需先 `$env:RUSTC_WRAPPER=$null`。

### 三、任务 2：HTTPS/HTTP 总长探测 + 完成回填（P1）

- `crates/motrix-core/src/kget.rs`：`probe_content_length` 由手写 TCP 重写为 **reqwest**（HEAD → Content-Length；HEAD 不可用时 `Range: bytes=0-0` GET 从 Content-Range 兜底；超时 5s；失败返回 `Ok(0)` 不阻塞任务；独立 std 线程规避 tokio 上下文 drop runtime panic）。
- **细化**：探测请求显式发送 `Accept-Encoding: identity`——实测 jsdelivr/Cloudflare 的 Content-Range 返回的是**压缩后缓存大小**（26751 vs 真实 73015），identity 保证拿到真实文件大小。
- `crates/motrix-core/src/engine.rs`：新增 `resolve_finished_bytes`，`handle_finished` 在 `total_length == 0`（探测失败）时读取磁盘文件大小回填 `total_length` / `completed_length`（进度=100%）。

### 四、任务 3：原生 shell 能力 command（P2）

- `src-tauri/src/commands.rs` 新增并注册三个 command（对齐 `src/shims/remote-shell.js` 既有调用）：
  - `show_item_in_folder`：Windows `explorer /select,<path>`、macOS `open -R`、Linux 打开父目录（无标准"定位文件"命令，注释说明限制）；
  - `open_path`：Windows `explorer`、macOS `open`、Linux `xdg-open`；
  - `trash_item`：`trash` crate（v5.2.6）移入回收站。
- 平台命令构造提取为纯函数 `build_shell_command`（可跨平台单测）；自定义 command 无需新增 capability。
- 前端"在文件夹中显示 / 打开文件 / 删除到回收站"恢复可用。

### 五、任务 4：下载速度统计（P3）

- `crates/motrix-core/src/engine.rs`：`TaskManager` 新增 `speed_samples` 快照表，进度回调按 `Δcompleted / Δt`（每 100ms 采样）自算瞬时 `download_speed`；`total == 0` 时不计算；完成/暂停/失败/移除时清除快照并归零。

### 六、任务 5：全量验证与回归

- `cargo test --workspace`：**80 用例全绿**（46 单元 + 6 契约 + 4 引擎集成 + 3 KGet 集成 + 6 RPC 协议 + 12 RPC JSONRPC + 3 shell command），无回归。
- `npm run build:web` 成功；前端 `src/` 与 capabilities 零改动（git 核对）。
- 已更新 `migrate-to-tauri-phase2/checklist.md` 受影响验收项（进度/速度/用例数标注修复）。

### 七、前端 Web 构建兼容修复（本周期追加）

Tauri WebView 加载前端时控制台连续报错导致**页面空白**，三层致命错误逐一修复：

| # | 报错 | 根因 | 修复 |
| --- | --- | --- | --- |
| 1 | `require is not defined` @ index.html | `src/index.ejs` 把 Electron dev 专用 `require('module')` 内联脚本输出到了 Web 页面 | 模板加 `!htmlWebpackPlugin.options.isBrowser` 条件 |
| 2 | `process is not defined` @ constants.js | web 构建无 Node `process`，`process.env.PORTABLE_EXECUTABLE_DIR` / `NODE_ENV`（vue、store 引用）未替换 | `.electron-vue/webpack.web.config.js` DefinePlugin 全量替换为字面量 |
| 3 | `Buffer is not defined` @ bencode | `parse-torrent → bencode`（SelectTorrent.vue 顶层引入）需要 Node `Buffer`，webpack 5 不内置 | 新增 `buffer` 依赖（v5.7.1）+ ProvidePlugin 全局注入 + resolve.fallback |

**浏览器实测**（browser_use）：三个 ReferenceError 全部消失（require 内联脚本移除、`process.env.*` 全替换、Buffer 注入 13 处）；纯浏览器显示骨架屏为预期（无 Tauri 后端导致 invoke 失败），真实 Tauri 应用内有后端会正常挂载。

### 八、端到端实测（真实应用，经 JSON-RPC）

添加真实 https 任务 `https://proof.ovh.net/files/10Mb.dat`：

| 指标 | 修复前 | 实测值 |
| --- | --- | --- |
| https 任务 totalLength | 恒 0 | **10485760**（10MB，探测正确） |
| downloadSpeed | 恒 0 | **9155764**（峰值 ~8.7MB/s） |
| 完成进度 | 0 / 0 | **complete，completedLength == totalLength** |

下载文件落盘大小精确 10485760 字节，任务与演示文件已清理。

### 九、已知限制与后续事项

- **服务器侧行为（非缺陷）**：w3.org 被 Cloudflare 对 reqwest 客户端返回 403（反爬），此类站点探测返回 0 且 KGet 亦无法下载；压缩型 CDN（jsdelivr）的 Content-Range 为压缩缓存大小，KGet 完整性校验可能报 size mismatch。
- **KGet 库限制**：进度回调无字节数、无实时速度（已由本次自算速度覆盖）；`max-connection-per-server` 运行中任务即时生效受限。
- **待后续阶段（Phase 3-6）**：BT（librqbit）、bitfield 降采样、peers 分页；托盘/菜单/深链/自启/UPnP/更新/单实例；motrix-cli；JSON-RPC WS 对外通知推送（notify.rs 接 WS）；FTP/SFTP 集成测试；性能基准与打包发布。

### 附：本次变更文件清单

| 文件 | 变更 |
| --- | --- |
| `crates/motrix-core/Cargo.toml` | 新增 reqwest 依赖 |
| `crates/motrix-core/src/kget.rs` | probe 重写（reqwest + identity）+ 相关测试 |
| `crates/motrix-core/src/engine.rs` | 完成回填、速度统计、相关测试 |
| `src-tauri/Cargo.toml` | 新增 trash 依赖 |
| `src-tauri/src/commands.rs` | 3 个 shell command + 纯函数 + 测试 |
| `src-tauri/src/lib.rs` | 注册新 command |
| `src/index.ejs` | 移除 Web 构建的 require 内联脚本 |
| `.electron-vue/webpack.web.config.js` | DefinePlugin / ProvidePlugin / buffer fallback |
| `package.json` | 新增 buffer 依赖 |
| `README-CN.md` | Rust 构建前置说明（perl / MSVC） |
| `Cargo.lock` | 随依赖更新 |

---

*文档维护：随开发阶段滚动更新。*
